- categories slug and post from strapi
- portfolio page (similar to blog function, content pull from strapi)

minor changes
- uses strapi block renderer to render rich text blocks
- theme color
- play around with seo setup

(npm bug while installing blocks-react-renderer)
might have issue while deploying to production... 
solutions: 
npm install -g husky or npm install --ignore-script @strapi/blocks-react-renderer